#include <bits/stdc++.h>
using namespace std;

#define IOS                  \
    ios::sync_with_stdio(0); \
    cin.tie(0);              \
    cout.tie(0);
#define int long long

signed main()
{
    string str;
    cin >> str;
    int k;
    cin >> k;
    unordered_map<string, int> mp;
    for (int i = 0; i < k; i++)
    {
        string s;
        cin >> s;
        mp[s]++;
    }
    for (int i = 0; i < str.size(); i++)
    {
        
    }
    return 0;
}